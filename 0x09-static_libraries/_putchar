#include <unistd.h>

/**
 * _putchar - writes the cahracter c to stdout
 * @c: character to print
 *
 * Return: On success 1.
 * on error, -1  is returned, and erno is set
 */
int _putchar(char c)
{
return (write(1, &c, 1));
}
