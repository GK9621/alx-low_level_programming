#include <stdio.h>

/**
 * main - write program that prints name
 * Renaming program will print new name
 * path before name should be removed
 *
 * @argc: count argument
 * @argv: vector argument
 *
 * Return: return to 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
